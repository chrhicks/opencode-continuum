# Arbeit (Agent Guide)

Arbeit is an agent-first task system that stores work context so tasks can be resumed across sessions.

To begin, call `arbeit_init` before any other tool. The init response includes a markdown quickstart with best practices for how to proceed.
